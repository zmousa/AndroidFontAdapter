Android Font Adapter
====================
Simple Android application to change font using `ttf` files.

Features
--------
 * Use shared preferences to save selected font.
 * Change font for each view component.